<nav class="navbar navbar-default">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#mobile" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Bill Manager</a>
        </div>
        <div class="collapse navbar-collapse" id="mobile">
            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ path_for('index') }}"><i class="fas fa-home"></i> Home</a></li>
                <li><a href="{{ path_for('getReading') }}"><i class="fas fa-tachometer-alt"></i> Readings</a></li>
                <li><a href="{{ path_for('myTransactions') }}"><i class="fas fa-chart-pie"></i> Transactions</a></li>
                <li><a href="{{ path_for('myProfile') }}"><i class="fas fa-user"></i> Profile</a></li>
                {% if function.getUserInfo(session.get('uid')).access == 1 %}
                <li><a href="{{ path_for('adminIndex') }}"><i class="fas fa-cogs"></i> ACP</a> </li>
                {%  endif %}
                <li><a href="{{ path_for('logout') }}"><i class="fas fa-power-off"></i> Logout</a></li>
            </ul>
        </div>
    </div>
</nav>